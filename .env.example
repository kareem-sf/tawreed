# Tawreed does not read provider API keys from environment variables.
# Enter credentials in Settings so they are stored by the OS credential manager.

# Optional engine diagnostics
# TAWREED_LOG_LEVEL=INFO
# TAWREED_LOG_DIR=~/.tawreed/logs
